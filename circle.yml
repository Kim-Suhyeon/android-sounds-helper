dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "tools,platform-tools,android-23,extra-android-support,extra-android-m2repository,extra-google-google_play_services,extra-google-m2repository"
    - echo y | android update sdk --no-ui --all --filter "build-tools-23.0.3"

test:
  override:
    # run lint (tests will come next ...)
    - ./gradlew :library:check

    # copy results
    - cp -r library/build/reports library/build/outputs $CIRCLE_ARTIFACTS